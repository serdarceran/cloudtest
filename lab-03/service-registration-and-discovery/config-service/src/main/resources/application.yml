spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/serdarceran/cloudtest
          search-paths: lab-03/service-registration-and-discovery/config-service/src/main/resources/client-configs
          force-pull: true
#          'lab-03/service-registration-and-discovery/config-service/src/main/resources/client-configs','test2'

server:
  port: ${vcap.application.port:9008}

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/

---
spring:
  profiles: cloud
eureka:
  instance:
    hostname: ${APPLICATION_DOMAIN}
    nonSecurePort: 80
